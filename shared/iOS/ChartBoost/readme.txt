Download the SDK from ChartBoost, and replace this directory with it.  (Don't change this directory name though, it should just contain ChartBoost.h and libChartBoost.a)

Example of usage in RTAdTest